package com.ctsousa.econcilia.service;

import com.ctsousa.econcilia.ApplicationUnitTest;
import com.ctsousa.econcilia.model.dto.GraficoVendaUltimo7DiaDTO;
import com.ctsousa.econcilia.graphic.GraficoVendaSemanalAcumulada;
import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.Test;

import java.math.BigDecimal;

class GraficoVendaUltimo7DiasServiceTest extends ApplicationUnitTest {

    @Test
    void deveGerarGraficoVenda() {
//        GraficoVendaService<GraficoVendaUltimo7DiaDTO> grafico = new GraficoVendaSemanalAcumulada();
//
//        GraficoVendaUltimo7DiaDTO graficoDTO = grafico.processar(getVendas());
//
//        Assertions.assertEquals(7, graficoDTO.getData().size());
//        Assertions.assertEquals(7, graficoDTO.getLabels().size());
//
//        Assertions.assertEquals(BigDecimal.valueOf(3840D), graficoDTO.getData()
//                .stream()
//                .filter(v -> v.compareTo(BigDecimal.ZERO) > 0)
//                .toList().get(0));
    }
}
